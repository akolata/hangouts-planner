spring:
  application:
    name: hangout-service
  config:
      import: "optional:configserver:"
  cloud:
    config:
      fail-fast: true
      username: config-client
      password: JnJc&4Y+9:+vFpmg